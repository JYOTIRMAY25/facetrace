"""FaceTrace test suite.

STEP 1 tests prove the scaffolding is real: every module imports, configuration
loads from an explicit environment mapping, the result models behave, and the
placeholder services satisfy their protocols while refusing to invent data.
"""
